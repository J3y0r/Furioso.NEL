<Page
    x:Class="FuriosoNEL.Page.PluginsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FuriosoNEL"
    xmlns:handlers="using:FuriosoNEL.Handlers.Plugin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:BoolNegationConverter x:Key="BoolNegationConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <!-- Card Container -->
        <Grid CornerRadius="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Card Title / Header -->
            <Grid Grid.Row="0" Padding="20,20,20,12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="已安装插件" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Content="重启" Click="RestartButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- Card Body -->
            <Grid Grid.Row="1" Padding="20">
                <!-- Empty State -->
                <TextBlock Text="暂无已安装插件" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="{x:Bind Plugins.Count, Converter={StaticResource CountToVisibilityConverter}, Mode=OneWay}"/>

                <!-- Plugin List -->
                <ListView ItemsSource="{x:Bind Plugins}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="handlers:PluginViewModel">
                            <Grid Padding="0,12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Info -->
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind Version}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="→" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Visibility="{x:Bind NeedUpdate, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"/>
                                        <TextBlock Text="{x:Bind LatestVersion, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Green" Visibility="{x:Bind NeedUpdate, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Actions -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <Button Content="卸载" 
                                            Click="UninstallPluginButton_Click" 
                                            Tag="{x:Bind}"
                                            Background="Red" 
                                            Foreground="White"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
        <Button Content="打开目录" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="OpenPluginsFolderButton_Click"/>
    </Grid>
</Page>
